# Encrypted_Message_Generator
A simple console app used for generate encrypted message using a cipher technique

## How to use
1. To run locally install the exectutable file for windows or run the `node app.js` locally
